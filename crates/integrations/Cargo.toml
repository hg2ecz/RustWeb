[package]
name = "integrations"
version = "0.1.0"
edition = "2024"

[dependencies]
ipnet = "2"
rustls = "0.23"
tokio = { version = "1", features = ["net", "io-util", "time", "sync"] }
tokio-rustls = "0.26"
webpki-roots = "1"
serde = { version = "1", features = ["derive"] }
toml = "0.9"
libc = "0.2"
